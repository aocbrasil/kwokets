services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB:       ticketing
      POSTGRES_USER:     ticketing
      POSTGRES_PASSWORD: ticketing_local
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
